extends: ["spectral:oas"]
rules:
  # Aturan: Wajib ada deskripsi di setiap API
  description-is-mandatory:
    description: "Semua API wajib memiliki deskripsi untuk dokumentasi."
    given: "$"
    severity: error
    then:
      field: info.description
      function: truthy
